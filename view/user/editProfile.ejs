<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Profile</title>
  <!-- Bootstrap CSS -->
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <!-- Custom CSS -->
  <link rel="stylesheet" href="styles.css"> <!-- Link to your custom CSS file -->
</head>
<body>
  <div class="container mt-5">
    <h1 class="text-center mb-4">Edit Profile</h1>
    <form id="editProfileForm" action="/editProfile" method="POST" novalidate>
      <div class="form-group">
        <label for="username">Username:</label>
        <input type="text" class="form-control" id="username" value="<%=loadData.name %>" name="username" required minlength="3" maxlength="20">
        <span id="errorName" style="color: red;"></span>
      </div>
      <div class="form-group">
        <label for="phone">Phone Number:</label>
        <input type="tel" class="form-control" id="phone" value="<%=loadData.mobile%>" name="phone" pattern="[0-9]{10}" required>
        <span id="errorMobile" style="color: red;"></span>
        
      </div>
      <button type="submit" class="btn btn-primary">Update Profile</button>
    </form>
  </div>

  <!-- Bootstrap JS and dependencies -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  <!-- <script>
   
    (function() {
      'use strict';
      window.addEventListener('load', function() {
        var form = document.getElementById('editProfileForm');
        form.addEventListener('submit', function(event) {
          if (form.checkValidity() === false) {
            event.preventDefault();
            event.stopPropagation();
          }
          form.classList.add('was-validated');
        }, false);
      }, false);
    })();
  </script> -->

  <script>
    const editForm=document.getElementById('editProfileForm')
    editForm.addEventListener('submit',(event)=>{
      event.preventDefault()
      const name=document.getElementById('username').value
      const mobile=document.getElementById('phone').value

       const errorName=document.getElementById('errorName')
       const errorMobile=document.getElementById('errorMobile')
       
       errorName.innerHTML='';
       errorMobile.innerHTML='';

       if(name.trim()==''){
        errorName.innerHTML='Please enter the name';
        return false
       }
       const phoneRegex = /^\d{10}$/;
       if(!phoneRegex.test(mobile)){
        errorMobile.innerHTML='Please enter a valid 10-digit mobile number';
        return false
       }
       editForm.submit();

    })

  </script>

  <style>
    /* styles.css */
body {
  background-color: #f8f9fa;
}

.container {
  max-width: 600px;
  margin-top: 50px;
}

h1 {
  color: #343a40;
}

.btn-primary {
  background-color: #007bff;
  border-color: #007bff;
}
  </style>
</body>
</html>